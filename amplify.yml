version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Skipping build steps, serving from public folder"
  artifacts:
    baseDirectory: public    # Point to the public folder
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
